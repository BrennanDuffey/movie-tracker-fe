import BigCard from './BigCard';
import { shallow } from 'enzyme';

describe('BigCard', () => {
  it.skip('should redner to the dom correctly', () => {
    let wrapper = shallow(<BigCard />);
  });
});