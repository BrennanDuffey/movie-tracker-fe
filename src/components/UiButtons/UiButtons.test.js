import UiButtons from './UiButtons';
import React from 'react';
import { shallow } from 'enzyme';

describe('UiButtons', () => {
  it.skip('should render to the dom correctly', () => {
    let wrapper = shallow(<UiButtons />);
  });
});